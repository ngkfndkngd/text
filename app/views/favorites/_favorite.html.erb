<% if @recipe.favorited_by?(current_user) %>
  <p class="favorite-title"><i class="fas fa-heart"></i>
  <%= link_to "レシピ保存済", recipe_favorite_path(@recipe), method: :delete, remote: true, class:"link-recipe" %>
  </p>
<% else %>
  <p class="favorite-title"><i class="far fa-heart"></i>
  <%= link_to "レシピ保存", recipe_favorites_path(@recipe), method: :post, remote: true, class:"link-recipe" %>
  </p>
<% end %>

